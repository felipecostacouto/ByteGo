package ToDeleteLater;

import java.util.ArrayList;

public class PersonService
{
//    PersonDao personDao = new PersonDao();
//
//    public double getBMI(String fullName)
//    {
//        System.out.println(">>> Trying to get BMI from name " + fullName);
//        ArrayList<Person> people = personDao.findBy(fullName);
//        if (people.size() == 0) return 0.0;
//        return people.get(0).getWeight() / (people.get(0).getHeight() * people.get(0).getHeight());
//    }
}
